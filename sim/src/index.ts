/**
 * Reflux simulation harness entrypoint (qualification gate — Phase 6).
 *
 * Pipeline: fetch-history → engine replay → stress scenarios → SIMULATION.md.
 * Until Phase 6 this binary refuses to run rather than emit a fake report.
 */
console.error('reflux-sim: not implemented until Phase 6 — refusing to run.');
process.exit(1);
