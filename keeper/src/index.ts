/**
 * Reflux keeper service entrypoint.
 *
 * Implemented in Phase 5 (watcher, roller, ltv-watchdog, healthcheck).
 * Until then this binary refuses to start rather than pretend to run.
 */
console.error('reflux-keeper: not implemented until Phase 5 — refusing to start.');
process.exit(1);
